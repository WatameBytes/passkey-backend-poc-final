# Oracle DataSource configuration
spring.datasource.url=jdbc:oracle:thin:@localhost:1522/FREEPDB1
spring.datasource.username=system
spring.datasource.password=mypassword1
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.Oracle12cDialect

# MongoDB configuration
spring.data.mongodb.uri=mongodb://localhost:27018/data
#spring.data.mongodb.username=mongo_user  # Uncomment and set these if you have MongoDB authentication enabled
#spring.data.mongodb.password=mongo_password

# Application-specific configurations
app.relying-party-id=localhost
app.relying-party-name=Example Application

# Needed for CORS for our RP
app.relying-party-origins=https://localhost:3000

app.relying-party-appId=https://localhost:8080
